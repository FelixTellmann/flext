/*= =============== Strings ================ */
let name: string = 'Felix Tellmann';
let title: string = "Senior Full-stack Developer";
let website: string = String("https://flext.dev")
let greeting: string = `Hello, my name is ${name}, and I'm a ${title}.`

// You can use single-, double-quotes or backticks to set
// a variable to type string. You can also use the String()
// keyword.

let inferredName = "Felix Tellmann"
// inferredName: string

let inferredTitle = "Senior Full-stack Developer";
// inferredTitle: string

let inferredWebsite = String("https://flext.dev")
// inferredWebsite: string

let inferredGreeting = `Hello, my name is ${name}, and I'm a ${title}.`
// inferredGreeting: string
